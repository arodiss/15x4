<nav class="navbar navbar-default" style="margin-bottom: 0;">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path("Landing") }}"><strong>15x4</strong></a>
        </div>

        <div class="collapse navbar-collapse" id="navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path("About") }}">О проекте</a></li>
                <li class="dropdown">
                    <a
                        href="#"
                        class="dropdown-toggle"
                        data-toggle="dropdown"
                        role="button"
                        aria-haspopup="true"
                        aria-expanded="false"
                    >
                        Смотреть лекции
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path("LectureList") }}">Все лекции</a></li>
                        <li class="divider" role="presentation"></li>
                        <li><a href="{{ path("LectureRandom") }}">Случайная лекция</a></li>
                        <li class="divider" role="presentation"></li>
                        <li><a href="{{ path("Fields") }}">Дисциплины</a></li>
                        <li><a href="{{ path("Tags") }}">Теги</a></li>
                        <li><a href="{{ path("Cities") }}">Города</a></li>
                        <li><a href="{{ path("Lecturers") }}">Лекторы</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    {% if is_granted('ROLE_USER') %}
                        <div class="pull-right">
                            <img style="position: absolute; top: 0; border-radius: 25px; height: 50px; width: 50px;" src="{{ app.user.pictureUrl }}" alt=""/>
                        </div>
                    {% else %}
                        <a href="{{ path('hwi_oauth_connect') }}">Авторизоваться</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>
