{% extends "admin/list-layout.html.twig" %}

{% block admin_list_content %}
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>Название</th>
                <th>Категория</th>
                <th>Лектор</th>
                <th>Ивент</th>
                <th>Теги</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            {% for lecture in pagination.items %}
                <tr>
                    <td>{{ lecture.title }}</td>
                    <td>{{ lecture.field }}</td>
                    <td>{{ lecture.lecturer }}</td>
                    <td>{{ lecture.event.city }}, {{ lecture.event.date | date }}</td>
                    <td>{{ lecture.tags | join(", ") }}</td>
                    <td>
                        <a href="{{ path("AdminLectureEdit", { "id": lecture.id }) }}"><span class="glyphicon glyphicon-edit"></span></a>
                        <a class="delete" href="{{ path("AdminLectureDelete", { "id": lecture.id }) }}"><span class="glyphicon glyphicon-trash"></span></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
            <tr><td colspan="6">
                {% include 'shared/pagination.html.twig' with { pagination: pagination.paginationData } %}
            </td></tr>
        </tfoot>
    </table>
{% endblock %}

{% block before_form %}
    <div class="alert alert-info">
        <span class="glyphicon glyphicon-star"></span>
        Если вы создавали анонс лекции, которую сейчас хотите загрузить - то не делайте этого здесь.
        <br/>
        Вместо этого найдите нужный анонс в <a href="{{ path('AdminAnnouncements') }}">списке</a> и нажмите напротив встречи / лекции кнопку "залить видео"
    </div>
{% endblock %}
